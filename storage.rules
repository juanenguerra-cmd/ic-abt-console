rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Only authenticated users may read or write their own storage paths.
    match /users/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
